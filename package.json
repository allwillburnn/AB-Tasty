{
    "scripts": {
        "run-tests-headed": "npx cypress run --headed",
        "run-tests-headless": "npx cypress run"
    },
    "devDependencies": {
        "cypress": "^12.17.4",
        "cypress-xpath": "^2.0.1",
        "typescript": "^5.2.2"
    }
}
